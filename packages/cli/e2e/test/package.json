{
  "name": "test",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run -b -F './packages/*' build && bun run -b -F './apps/*' build",
    "dev": "bun run build && bun run -b -F '*' dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}